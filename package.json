{
  "name": "banner-of-nations",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "scripts": {
    "start": "serve -s build -l $PORT",
    "build": "vite build",
    "preview": "vite preview"
  }
}
